# ─────────────────────────────────────────────
# CandidateFusion – Production Dependencies
# Python 3.11+
# ─────────────────────────────────────────────

# Core validation & serialization
pydantic[email]>=2.6.0,<3.0.0

# Phone number normalization
phonenumbers>=8.13.0

# Flexible date parsing
python-dateutil>=2.9.0

# PDF text extraction (PyMuPDF)
PyMuPDF>=1.24.0

# GitHub REST API client
PyGithub>=2.3.0

# HTTP client (used by github parser fallback)
requests>=2.31.0

# CLI argument parsing (stdlib augmentation)
# argparse is stdlib but we pin rich for pretty output
rich>=13.7.0

# JSON schema (for config validation)
jsonschema>=4.21.0

# Fuzzy skill matching
rapidfuzz>=3.6.0

# Logging extras
python-json-logger>=2.0.7

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-mock>=3.12.0

# Type checking (dev)
mypy>=1.9.0
types-requests>=2.31.0
types-python-dateutil>=2.9.0

# Code quality (dev)
ruff>=0.3.0
